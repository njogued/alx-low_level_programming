#include "main.h"
/**
 * _strlen - Find string length
 * @s: - string
 * Return: int with string length
 */
int _strlen(char *s)
{
	int a;
	int i;

	a = 0;
	for (i = 0; a < s[i]; i++)
	{
		i = i;
	}
	return (i);
}
