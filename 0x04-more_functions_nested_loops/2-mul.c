#include "main.h"
/**
 * mul - function to print mutli of two
 * @a: first integer input
 * @b: second integer input
 * Return: The multiple
 */
int mul(int a, int b)
{
	int reve;

	reve = a * b;
	return (reve);
}
